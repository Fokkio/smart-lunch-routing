# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- เจ้าของร้านข้าวกล่อง ใช้จอคอมพิวเตอร์จัดการลูกค้า ออเดอร์ และแผนส่งช่วงก่อนเที่ยง โดยไม่ควรต้องเข้าใจศัพท์เทคนิค
- ไรเดอร์ ใช้มือถือเปิดใบงานของตนและอ่านจุดส่งตามลำดับ

## Product Purpose

เดโมช่วยร้านจัดกลุ่มออเดอร์ให้ไรเดอร์ ตรวจเวลาส่งก่อน 12:30 และเห็นต้นทุนกับกำไรที่คาดการณ์ได้ในหน้าจอเดียว

## Operating Context

- อาหารพร้อมออกส่งราว 11:30; เส้นตาย 12:30
- ประมาณ 20–30 ออเดอร์ต่อวัน รายละไม่เกิน 3 กล่อง ไรเดอร์รับไม่เกิน 3 ออเดอร์ต่อเที่ยว
- จุดส่งอยู่แถวมหาวิทยาลัยมหาสารคาม; แผนที่ออนไลน์ใช้ Leaflet และ OpenStreetMap
- พิกัด ชื่อ และเบอร์โทรในเดโมเป็นข้อมูลสมมติ

## Capabilities and Constraints

- Angular frontend เท่านั้น ไม่มี Backend หรือฐานข้อมูล Server; ข้อมูลลูกค้าและออเดอร์เดิมเก็บใน localStorage
- เส้นทางเป็น heuristic ระยะเส้นตรง ไม่ใช่ระยะถนนหรือผลคำนวณที่ดีที่สุดทางคณิตศาสตร์
- ผู้ใช้ขอให้ flow ที่ข้อความแนบเพิ่ม—ยืนยันแผน ใบงาน การกดส่งสำเร็จ และประวัติ—เป็นหน้าจอ UI เท่านั้น ไม่ต้องบันทึกสถานะจริง
- รหัสใบงานเป็นกลไกเดโม ไม่ใช่ authentication สำหรับ Production
- ค่าอาหาร 65 บาท/กล่อง ต้นทุน 40 บาท/กล่อง; ค่าไรเดอร์ 15 บาท/เที่ยว บวก 2 บาทต่อกิโลเมตรต่อกล่องที่ขน

## Brand Commitments

- ใช้ภาษาไทยเป็นหลัก ข้อความสั้น เข้าใจได้ทันที
- ชื่อในระบบเดิมคือ “ครัวเที่ยงตรง”; ชื่อโครงการในเอกสารใหม่คือ “ระบบจัดเส้นทางและแบ่งงานไรเดอร์อัจฉริยะ”

## Evidence on Hand

- ข้อกำหนดหลักเดิม: `Project.pdf` ที่ผู้ใช้แนบในรอบก่อน
- ข้อความ UX/UI เพิ่มเติม: `Pasted text.txt` ที่แนบในรอบนี้
- แอป Angular ที่ทำงานได้แล้วใน `D:\AdvanceWebProject`

## Product Principles

- เจ้าของร้านเห็นสถานะและสิ่งที่ต้องทำต่อก่อนตัวเลขเชิงเทคนิค
- ไรเดอร์เห็นงานของตัวเองทีละจุด ไม่เห็นบัญชีร้านหรืองานคนอื่น
- แสดงข้อจำกัดของเดโมอย่างตรงไปตรงมา ไม่อ้างว่าเป็นระบบนำทางหรือยืนยันตัวตนจริง
